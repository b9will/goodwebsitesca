Checkbox with a sun-gold fill and a springy tick on check.

```jsx
<Checkbox label="Keep me posted" defaultChecked />
<Checkbox label="I agree to the good vibes" />
```

Props: `label` plus native checkbox attrs (`checked`, `defaultChecked`, `onChange`, `disabled`).
