Toggle switch. Ink thumb slides on a track that turns grass-green when on.

```jsx
<Switch label="Show prices in USD" defaultChecked />
```

Props: `label` plus native checkbox attrs (`checked`, `defaultChecked`, `onChange`, `disabled`). Uses `role="switch"`.
