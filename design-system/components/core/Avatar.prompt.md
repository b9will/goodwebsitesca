Round, outlined avatar. Shows a photo or auto-coloured initials from the name.

```jsx
<Avatar name="Posy Field" src="/team/posy.jpg" size="lg" />
<Avatar name="Ravi Shah" />
```

Sizes: `sm` (32), `md` (44), `lg` (60). Colour is picked from the name when no `src` is given.
