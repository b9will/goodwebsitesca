Small pill for statuses, categories and counts.

```jsx
<Badge variant="sun">New</Badge>
<Badge variant="success" dot>Live</Badge>
<Badge variant="outline">Web design</Badge>
```

Variants: `solid`, `sun`, `outline`, `soft`, plus status `success` / `info` / `warning` / `danger`. Set `dot` for a leading status dot.
